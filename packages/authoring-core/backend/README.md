# authoring-core (backend)

Shared backend primitives (routers + validation helpers) to be extracted from Atlas and reused by Forge.

## Planned extractions
- CRUD router factory
- generation settings + validation helpers
- shared lookup routes
